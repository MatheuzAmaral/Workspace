const menu = document.querySelecetor('.menu');
const NavMenu = document.querySelecetor('.nav-menu');

menu.addEventListener('click, {) =>'{
    menu.classlist.toggle('ativo');
    NavMenu.classlist.toggle('ativo');
})